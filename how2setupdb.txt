create a table called bookmarks with columns:
    - database_id (generated)
    - channel_id BIGINT
    - message_id BIGINT
    - bookmark_owner_id BIGINT

create a table called prefixes with columns:
    - prefix TEXT
    - owner_id BIGINT

create a table called semi-cached-bookmarks with columns:
    - content TEXT
    - database_id BIGINT
    - jump_url TEXT